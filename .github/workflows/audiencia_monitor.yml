name: Monitor de Audiências

on:
  schedule:
    - cron: '0 15 * * *'  # 12h BRT (UTC-3) todos os dias
  workflow_dispatch:

jobs:
  audiencia-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Executar Monitor de Audiências
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASS: ${{ secrets.GMAIL_PASS }}
        run: python scripts/audiencia_monitor.py
